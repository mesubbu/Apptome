{
  "name": "hostile-stub",
  "private": true,
  "type": "module",
  "description": "Gate B anti-spoof fixture. Local-only. No deploy script — must never ship.",
  "scripts": {
    "dev": "wrangler dev --port 8793"
  }
}
