# T6 — Transport 2 (thin extension) — 2026-08-29

Pinned unpacked id: `emdpceafindjgkgpgajjapoeklpjkogo` (manifest `key`). `config.js` and `origins.js` match. The id in the original plan was a placeholder; we did not guess a private key for it.

## T6.1

Unpacked load via Chrome `Extensions.loadUnpacked`. CRM/Ledger set `window.__CONNECTOME_EXTENSION__` at `document_start`. Page boot then uses `TRANSPORT.EXTENSION` (no DO WebSocket for payloads). Surface `connect()` prefers the extension and shows **on-device hub**.

Extension-owned JS is ~200 lines (`page-flag.js`, `isolated.js`, `boot.js`, `sw.js`). Vendor copies stay generated. No mapping, no confirm UI, no `<all_urls>`, no `chrome.cookies`. `host_permissions` are the three stubs + surface + gateway. `externally_connectable` is the surface origin only.

## T6.2

Invoke is `chrome.runtime.sendMessage` → SW → page-world `executeTool`. Gateway access log during a successful Gate B write: `POST /api/sync` (origins, tool names, schemas) and `POST /api/grant` (edge metadata). No `/hub` payload relay. Mapper `POST /map` remains schema-only. Confirm values (River North Studio, amount 180, invoice ids) do not appear as args on the gateway.

`chrome-extension://emdpceafindjgkgpgajjapoeklpjkogo` is allowed on `/api/*` only, not `/hub`.

## T6.3

Gate A six checks and Gate B items 1–8: **extension column green**. Same surface as edge; if they disagreed, the surface would be wrong.

Shots: `connectome/gates/gate-ab-ext/`.
